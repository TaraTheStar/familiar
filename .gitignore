# OS / editor cruft (component .gitignores under poppet/ and grimoire/
# handle build artifacts for each half)
.DS_Store
*.swp
.idea/
.vscode/

# Hugo (docs/ GitHub Pages site — local preview artifacts)
docs/public/
docs/resources/
docs/.hugo_build.lock
